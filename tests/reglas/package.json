{
  "name": "taquilla-reglas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tests de las reglas de Firestore contra el emulador. Instalación aparte: @firebase/rules-unit-testing exige una versión de firebase distinta de la que el repo clava para los tipos del CDN.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-taquilla \"node --test reglas.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
